// const Search = ({ value, onChange }) => (
//   <input placeholder="Type to filter..." value={value} onChange={onChange} />
// );

// const isSearched = searchTerm => item =>
//   item.question.toLowerCase().includes(searchTerm.toLowerCase());
